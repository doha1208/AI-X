# 개발 기준서

## 문서 목적

이 문서는 이 저장소에서 개발하기 전에 읽는 프로젝트 지도다. 기능이 어디에 구현되어 있는지, 어떤 데이터가 어떤 계산에 쓰이는지, 이미 적용된 제약이 무엇인지 한곳에 정리한다. 사용자 안내용 문서는 별도로 만들지 않으며, `README.md`는 명시적으로 요청받은 경우에만 작성하거나 수정한다.

새 기능을 추가하거나 기존 동작을 크게 바꾸면, 해당 기능의 책임 파일과 동작을 이 문서에도 함께 반영한다.

## 작업 규칙

- 이 작업공간에서는 커밋을 만들지 않는다.
- `.claude/`, `CLAUDE.md` 등 Claude 전용 파일은 읽거나 수정하지 않는다.
- 프런트엔드 코드를 수정하기 전에는 `frontend/AGENTS.md`와 설치된 Next.js 문서를 확인한다. 이 프로젝트는 Next.js 16을 사용한다.
- API 라우터는 요청 검증과 응답 조합에 집중하고, 점수·경로·외부 서비스 호출 같은 도메인 계산은 `backend/app/services/`에 둔다.
- 공공데이터 수집·정제·대량 생성 작업은 서버 요청 중에 하지 않고 `backend/scripts/`의 배치 스크립트로 분리한다.

## 서비스 개요

안심 거주지·귀갓길 추천 서비스다. 서울·경기 행정동의 공공 안전 데이터를 분석해 다음을 제공한다.

1. 안전지수가 높은 거주지 추천 Top 5
2. 주소 또는 지도 좌표를 기준으로 한 주변 안전구역 조회
3. CCTV, 보안등, 범죄율, 경찰서·비상벨 거리, 상점 수를 반영한 안전지수
4. OSM 보행자 도로망의 안전 가중 귀갓길과 최단경로 비교
5. 경로 주변 안전비상벨 표시와 위치 변화에 따른 실시간 재경로

프런트엔드는 카카오맵으로 정보를 표시하고, FastAPI 백엔드는 인증·점수 계산·경로 탐색을 제공한다. 배포 시 Caddy는 프런트와 API를 하나의 도메인의 `/api`로 프록시한다. 배포 전 로컬 개발은 `frontend/.env.local`의 API 주소 오버라이드로 백엔드에 직접 연결할 수 있다.

## 디렉터리와 책임

| 경로 | 책임 |
| --- | --- |
| `frontend/src/app/` | 페이지와 화면 상태. 대시보드, 로그인, 회원가입 |
| `frontend/src/components/` | 재사용 가능한 UI. 현재 핵심은 카카오 지도 `SafetyMap.tsx`와 SVG 아이콘 |
| `frontend/src/lib/` | API 호출, JWT 저장, 거리 계산, 카카오 SDK·지오코딩 |
| `frontend/public/data/` | 브라우저가 직접 읽는 서울·경기 행정동 경계 GeoJSON |
| `backend/app/api/` | HTTP API. `auth.py`는 인증, `safety.py`는 안전구역·비상벨·경로, `admin_scoring.py`는 허용 이메일 관리자의 점수 프로필·빌드 상태 |
| `backend/app/services/` | 안전 점수, 안전 경로, 도로 특성, 시설 밀도, 시간대, Tmap 등 핵심 로직 |
| `backend/app/models/` | SQLAlchemy DB 모델. 사용자와 안전구역 |
| `backend/app/schemas/` | API 요청·응답 Pydantic 모델 |
| `backend/app/core/` | 환경 변수 설정과 JWT 암호화 |
| `backend/app/db/` | DB 엔진·세션 생성 |
| `backend/app/data/` | 런타임에 읽는 행정동 격자와 샘플 데이터 |
| `backend/scripts/` | 공공데이터 수집·정제, OSM 도로망·POI·행정동 경계 생성 배치 |
| `backend/tests/` | 인증, 점수, 경로, 데이터 보정의 단위·통합 테스트 |
| `Caddyfile` | `ansimnavi.duckdns.org`에서 `/api/*`는 백엔드, 나머지는 프런트로 전달 |
| `run_app.cs` | 로컬에서 백엔드와 프런트를 함께 실행하기 위한 런처 소스 |

## 주요 실행 흐름

### 인증

`frontend/src/app/login/page.tsx`와 `signup/page.tsx`가 `frontend/src/lib/api.ts`를 통해 `/auth/signup`, `/auth/login`을 호출한다. 백엔드는 bcrypt 해시, HttpOnly access JWT 쿠키, DB에 해시만 저장하는 refresh 세션을 사용한다. refresh는 갱신마다 이전 세션을 폐기하고 새 식별자로 회전하며, 로그아웃도 현재 refresh 세션을 폐기한다. 상태 변경 요청은 읽기 가능한 CSRF 쿠키와 `X-CSRF-Token` 헤더를 일치시켜야 한다. 운영 쿠키는 host-only·Secure·SameSite=Lax이고, CORS는 credential 쿠키를 위해 정확한 origin만 허용한다.

### 거주지·주변 안전구역

대시보드 `frontend/src/app/page.tsx`가 다음 API를 사용한다.

- `GET /safety/residence-recommend`: DB의 저장된 주간 안전지수 기준 Top 5
- `GET /safety/zones`: 입력 좌표와 반경 안의 행정동 안전구역
- `GET /safety/bells`: 입력 좌표와 반경 안의 안전비상벨. 기본 최대 60개

`SafetyMap.tsx`는 각 동의 GeoJSON 경계가 있으면 폴리곤으로, 아직 없거나 실패했으면 원으로 표시한다. 경계 데이터는 `seoul-dong-boundaries.geojson`과 `gyeonggi-dong-boundaries.geojson`을 함께 읽는다.

### 안전지수

`backend/app/services/safety_score.py`가 동 목록 안에서 각 요소를 min-max 정규화해 0~100점으로 합산한다.

| 요소 | 안전한 방향 | 낮 가중치 | 밤 가중치 |
| --- | --- | ---: | ---: |
| CCTV 수 | 많을수록 | 0.25 | 0.15 |
| 보안등 수 | 많을수록 | 0.10 | 0.25 |
| 인구 1만 명당 범죄율 | 적을수록 | 0.30 | 0.25 |
| 가장 가까운 경찰서 거리 | 짧을수록 | 0.10 | 0.10 |
| 상점 수 | 많을수록 | 0.15 | 0.10 |
| 가장 가까운 비상벨 거리 | 짧을수록 | 0.10 | 0.15 |

밤 시간은 KST 기준 22:00~06:00이다. 보안등 데이터가 비정상적으로 적은 시·군·구는 `lights_known=False`로 표시하고, 보안등을 0개로 단정하지 않고 중립 점수 50으로 처리한다. 범죄율·경찰서 거리·비상벨 거리가 미수집이면 `None`으로 보존해 0으로 바꾸지 않고 같은 중립 점수로 처리한다. 범죄 원시 건수가 아니라 주민등록 인구로 보정한 범죄율을 사용한다.

### 완료 — 점수 프로필·산출물 운영

- [x] 허용 이메일 관리자만 점수 프로필 초안 저장·적용·빌드 상태 조회를 요청할 수 있다.
- [x] 적용 요청은 `queued → building → succeeded/failed` 대기열로 처리하며, 성공한 빌드만 활성 프로필과 `current` 산출물을 전환한다.
- [x] 실패·손상 산출물은 이전 활성 프로필과 산출물을 유지하고, API는 기존 점수 계산·Tmap·직선 폴백을 계속 제공한다.
- [x] 거주지 추천과 경로의 구역·폴백 점수는 유효한 활성 산출물의 시간대별 점수 맵을 우선 사용한다.
- [x] 관리자 화면에서 주·야간 가중치와 중립 점수를 검증해 초안을 만들고, 현재 데이터로 이전 프로필을 다시 적용할 수 있다.
- [x] Docker Compose의 API와 빌더는 DB·산출물 볼륨을 공유하며, 빌더는 30분 주기로 데이터 fingerprint와 활성 정책 변경을 확인한다.

### 안전 귀갓길

`POST /safety/route`는 `backend/app/api/safety.py`에서 세 경로 전략을 순서대로 사용한다.

1. `safety_weighted`: `safe_route.py`가 OSM 보행자 그래프에서 안전 비용이 낮은 경로를 찾는다. 직선거리 6km 이하는 최대 3개의 대안을, 그보다 길면 1개를 반환한다.
2. `tmap`: 안전 가중 탐색에 실패하면 Tmap 보행자 경로를 받아 점수만 표시한다.
3. `straight_line`: Tmap도 사용할 수 없으면 직선 5개 지점으로 근사한다.

안전 가중 경로의 간선 점수는 동 안전점수 40%, OSM 도로 특성 30%, 구간 주변 시설 밀도 30%다. 도로 특성은 도로 종류, OSM의 `lit`·`sidewalk` 태그, 막다른 길 여부를 반영한다. 시설 밀도는 구간 주변 CCTV·보안등의 가점과 교통사고 다발지점의 감점을 반영한다. 안전점수가 낮은 간선일수록 이동 비용을 최대 6배까지 늘려 경로 선택에서 피하게 한다.

`safe_route.py`의 배치 산출물 생성은 서울·경기 로컬 OSM 파일과 신뢰 가능한 pickle 그래프 캐시를 사용한다. API 요청은 생성된 산출물만 조회하며 Overpass를 호출하지 않는다.

공공데이터 적재가 끝나면 `backend/scripts/build_route_artifacts.py`를 실행해 낮·밤 안전 가중 그래프와 행정동 점수 맵을 버전 산출물로 게시한다. 산출물은 계산에 쓴 점수 프로필 버전과 안전 데이터 fingerprint를 함께 보관한다. API는 시작 시와 제한된 주기의 매니페스트 확인 때 최신의 검증된 산출물을 로드하며, HTTP 요청 중에는 OSM 다운로드나 간선 전체 점수화를 하지 않는다. 산출물이 없거나 손상되면 Tmap·직선 경로 폴백은 계속 제공한다.

관리자는 `/admin/scoring-profiles`에서 초안을 저장하고 적용 작업을 대기열에 넣는다. `artifact_builder_worker.py`는 `queued → building → succeeded/failed`를 처리하며, 게시와 활성 프로필 전환은 성공한 빌드에서만 일어난다. 실패·손상 산출물에서는 이전 `current` 산출물과 활성 프로필을 유지한다. `docker-compose.yml`의 API와 빌더는 DB·산출물 볼륨을 공유하고, 빌더는 30분마다 데이터 fingerprint와 활성 프로필을 확인해 변경된 경우에만 재생성 작업을 추가한다.

### 실시간 이동

대시보드는 `navigator.geolocation.watchPosition`으로 위치를 구독한다. 목적지에서 30m 이내면 안내를 끝내고, 마지막 경로 요청 위치에서 50m 이상 이동했으며 5초가 지났을 때만 재경로를 요청한다. 실패해도 기존 경로를 유지하고 다음 위치 갱신 때 다시 시도한다.

## 데이터 자산과 배치 작업

`backend/app/data/dong_grid.json`은 서울·경기 영역을 0.005도 격자로 나눈 뒤 카카오 좌표→행정동 API로 채운 캐시다. 현재 48,121개 격자와 1,024개 행정동(서울 425, 경기 599)을 담고 있다.

- `build_dong_grid.py`: 카카오 역지오코딩으로 행정동 격자를 생성·재개한다.
- `ingest_public_data.py`: CCTV, 보안등, 범죄, 인구, 비상벨, 사고다발지점 등을 수집·집계해 안전구역 데이터를 적재한다.
- `geocode_light_addresses.py`: 좌표가 없는 보안등 주소를 카카오 주소 검색으로 좌표화한다.
- `extract_seoul_walk_network.py`: 국내 OSM PBF에서 서울·경기 보행 가능 도로망을 추출한다.
- `extract_safety_pois.py`: 경찰서·파출소와 상점 POI를 추출한다.
- `build_gyeonggi_boundaries.py`: 필요한 경기도 행정동 경계를 단순화해 프런트 GeoJSON으로 만든다.

원본 대용량 데이터와 생성되는 OSM 파일은 `.gitignore`에서 제외한다. API 키와 실행 환경 값은 `backend/.env`, `frontend/.env.local`에 두며 예시 파일만 추적한다.

## 지금까지 추가된 기능

현재 브랜치와 코드 기준으로 확인한 주요 기능 추가 이력이다.

1. 안전지수 기반 거주지 추천과 귀갓길 추천의 기본 플랫폼
2. 이메일 회원가입·로그인, JWT access/refresh 토큰, 현재 사용자 조회
3. 로그인·회원가입 UI 개편
4. 카카오 주소 검색, 지도 클릭·우클릭 좌표 선택, 행정동 안전 지도
5. 안전 가중 경로와 Tmap·직선 폴백, 안전 경로와 최단경로 비교선
6. 야간 보안등 비중을 높이는 주·야간 안전점수와 KST 시간대 판정
7. 실시간 위치 추적과 일정 거리·시간 조건의 재경로 요청
8. 서울에서 서울·경기까지 데이터·도로망·행정동 경계 범위 확장
9. 범죄율의 인구 보정, 경찰서·상점·비상벨 거리 요인, 누락 보안등 데이터의 중립 처리
10. 도로 종류·인도·가로등·막다른 길과 구간 주변 CCTV·보안등·사고다발지점을 반영한 경로 점수
11. 지도상의 비상벨 마커와 경로 주변 비상벨 필터
12. 로컬 OSM 도로망·그래프 캐시·KD-tree 색인으로 경로 탐색 성능 개선
13. 데모용 경기 지역 범위 제한 환경 변수와 DuckDNS/Caddy 공개 프록시 설정
14. 백엔드·프런트를 함께 실행하는 로컬 런처 소스
15. 허용 이메일 관리자만 저장할 수 있는 안전점수 프로필 초안 API
16. 활성 산출물 기반 거주지·경로 점수, 대기열 기반 프로필 적용과 컨테이너 빌더 운영, 관리자 점수 프로필 화면

## 개선 로드맵

아래 순서는 기능 추가보다 공개 데모의 신뢰성·보안·정확성을 먼저 높이는 기준이다. 각 Phase의 완료 기준을 충족한 뒤 다음 단계로 진행한다.

### Phase 0 — 공개 데모 안정화

**목표:** 외부 사용자가 실제 서버 API를 호출하고, 기본 보안 설정과 과도한 경로 요청으로 인한 장애를 막는다.

| 작업 | 변경 대상 | 완료 기준 |
| --- | --- | --- |
| same-origin API 기본값 | `frontend/src/lib/api.ts`, `frontend/.env.local.example`, 배포 환경 변수 | 기본 API 주소는 `/api`이며, 로컬 직접 개발만 `frontend/.env.local`의 `http://localhost:8000` 오버라이드로 실행한다. |
| JWT 비밀키 fail-fast | `backend/app/core/config.py`, 시작 설정 | 개발 환경이 아닌 경우 `SECRET_KEY`가 없거나 기본값이면 서버가 시작되지 않는다. 배포 키는 충분히 긴 무작위 값으로 교체한다. |
| 경로 요청 좌표·거리 검증 | `backend/app/schemas/safety.py`, `backend/app/api/safety.py` | 전국 좌표를 허용하되 위도·경도 형식과 출발·도착 직선거리 10km 상한을 검증하고, 초과 시 명확한 422 응답을 준다. |
| 경로 계산 보호 | `backend/app/services/route_request_limit.py`, `safety.py` | 프로세스당 동시 계산 2건, IP당 분당 30건(초기 8건), 사용자당 분당 20건(초기 5건)을 적용하고 초과 시 `429`와 `Retry-After`를 반환한다. 신뢰된 로컬 프록시만 전달 IP를 사용하며, 식별자 버킷은 1만 개로 상한을 둔다. |

**선행 조건:** 최대 허용 보행 거리와 공개 API 정책을 확정한다. 지역은 전국 확장을 막지 않으며, 아직 경로 산출물이 없는 곳은 Tmap·직선 폴백을 사용한다.

### Phase 1 — 데이터와 경로 정확성

**목표:** 새 공공데이터가 거주지 추천과 경로 계산에 같은 기준으로 반영되고, 알 수 없는 데이터가 안전 점수를 왜곡하지 않게 한다.

**상태: 완료 (4/4).**

| 상태 | 작업 | 변경 대상 | 완료 기준 |
| --- | --- | --- | --- |
| ✅ | 경로 점수 캐시 무효화 | `backend/app/services/safe_route.py`, `backend/scripts/ingest_public_data.py` | 적재 완료 후 기존 안전점수 그래프·시설 색인이 안전하게 교체되며, 서버 재시작 없이 새 데이터 기준으로 경로를 계산한다. |
| ✅ | 누락 값의 중립 처리 | `backend/app/services/safety_score.py`, 모델·적재 스크립트, 관련 테스트 | 범죄율·경찰서 거리·비상벨 거리가 없는 동은 0으로 유리하게 계산되지 않고, 보안등과 같이 중립 점수로 처리된다. |
| ✅ | 로컬 OSM 자산 운영화 | 배포 설정, `safe_route.py`, 데이터 배포 절차 | 서울·경기 보행 그래프를 배포 볼륨 또는 관리형 아티팩트로 제공하고, 없을 때는 readiness 상태 또는 명확한 폴백 상태를 노출한다. |
| ✅ | 경로 실패 응답 정리 | `backend/app/api/safety.py`, 스키마·프런트 오류 처리 | 안전구역이 비어 있거나 외부 경로 서비스가 실패해도 500 오류 대신 원인과 가능한 다음 행동을 담은 응답을 준다. |

**선행 조건:** 데이터 갱신 주기와 그래프 파일 보관·배포 방식을 정한다.

### Phase 2 — 사용자 기능 완성

**목표:** 현재 구현된 인증·대안 경로·실시간 안내 기능을 화면과 서버 정책까지 일관되게 완성한다.

| 상태 | 작업 | 변경 대상 | 완료 기준 |
| --- | --- | --- |
| ✅ | 세션 보안 전환 | `frontend/src/lib/auth.ts`, 인증 API·의존성, CORS 설정 | access/refresh 토큰을 `localStorage` 대신 HttpOnly·Secure·SameSite 쿠키로 관리하고, 상태 변경 요청에는 CSRF 방어를 적용한다. |
| ✅ | refresh 토큰 수명주기 | `auth.py`, `security.py`, 프런트 인증 흐름 | 만료 access token은 안전하게 갱신되고, 로그아웃·탈취 대응을 위한 refresh token 폐기 또는 회전 정책이 있다. |
| ✅ | 대안 경로 UI | `frontend/src/lib/api.ts`, `page.tsx`, `SafetyMap.tsx` | API가 반환하는 대안 경로를 사용자가 비교·선택할 수 있고, 선택한 경로의 점수·거리·지도 선이 일치한다. |
| ✅ | 재경로 부가 정보 갱신 | `page.tsx`, 비상벨 API 연동 | 실시간 재경로 후 경로 주변 비상벨과 비교 경로가 새 경로 기준으로 갱신되며, 이전 비동기 요청 결과가 새 화면을 덮어쓰지 않는다. |

**선행 조건:** 로그인 필수 범위와 쿠키 기반 인증을 적용할 배포 도메인 정책을 확정한다.

### Phase 3 — 품질과 운영성

**목표:** 변경이 실제 기능을 망가뜨리지 않는지 빠르게 확인하고, 배포 환경의 문제를 관측·재현할 수 있게 한다.

| 작업 | 변경 대상 | 완료 기준 |
| --- | --- | --- |
| 테스트 격리 | `backend/tests/`, 테스트 fixture | 테스트가 import 시점에 실제 상대 DB를 삭제하지 않고, 임시 DB와 모의 그래프·Tmap 응답으로 재현 가능하게 실행된다. |
| 핵심 회귀 테스트 | backend·frontend 테스트 | 서비스 범위 검증, 캐시 갱신, 누락 데이터, `/api` 프록시, 토큰 만료·갱신, 재경로 시 마커 갱신을 자동 검증한다. |
| 배포 스모크 테스트 | 배포 스크립트 또는 CI | 공개 도메인에서 health, 로그인, 안전구역 조회, 경로 API가 올바른 origin과 환경 변수로 동작하는지 확인한다. |
| 관측성 | FastAPI 로깅·메트릭 설정 | 경로 계산 시간, 폴백 사유, 외부 API 실패, 캐시 적중률을 개인 위치·토큰을 기록하지 않고 확인할 수 있다. |

**선행 조건:** CI 실행 환경과 배포 후 점검에 사용할 비밀값 관리 방식을 정한다.

### 테스트 격리

`backend/tests/conftest.py`가 테스트 수집 전에 전용 임시 SQLite DB를 설정하고, 매 테스트마다 테이블을 초기화한다. `test_safety.py`의 안전구역 시드는 함수 단위 fixture로 넣는다. 종료 시 엔진 연결을 닫고 임시 DB를 제거하므로, Tmap TTL 캐시 테스트를 포함한 조합 실행도 DB 상태에 의존하지 않아야 한다.

## 변경할 때 확인할 지점

- API 응답을 바꾸면 `backend/app/schemas/`, `backend/app/api/`, `frontend/src/lib/api.ts`, 이를 소비하는 페이지·지도 컴포넌트를 함께 확인한다.
- 안전 점수 요인을 바꾸면 `safety_score.py`의 가중치·정규화와 `backend/tests/test_safety_score.py`를 함께 갱신한다.
- 경로 비용이나 도로 판단을 바꾸면 `safe_route.py`, `road_score.py`, `facility_density.py`와 관련 테스트를 함께 확인한다.
- 행정동 코드·경계·격자 범위를 바꾸면 백엔드 격자와 프런트 GeoJSON의 코드 체계가 일치하는지 확인한다.
- 외부 API 키가 없는 환경에서도 Tmap과 시설 데이터가 조용히 폴백하도록 유지한다.
